# web-dev-foundations

#Lors de ma premiere mission http j'ai fait face a un probleme de bug partilier
#Le probleme etait du a un probleme de casse qui empechait l'execution normale de mon script
#Au d'ecrire cette instruction "user-agent" il faut plutot ecrire "User-Agent" ou -i "user-agent" apres le grep
#mais l'instruction le plus conseiller c'est "User-Agent",car elle respecte la norme de casse
